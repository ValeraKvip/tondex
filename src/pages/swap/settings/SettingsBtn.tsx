import { ReactComponent as Settings } from '../../../icons/settings.svg';
import './setting.scss';
export default function SettingBtn() {
    return (
        <Settings className="settings-btn"></Settings>
    );
}